<% if (locals.errors && locals.errors.length > 0) { %>
    <div class="errors">
        <strong>Error!</strong>
        <% locals.errors.forEach((error) => { %>
            <p><%= error.msg %></p>
        <% }); %>
    </div>
<% } %>
