<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Clubhouse</title>
</head>
<body>
    <%- include('partials/header') %>
    <div id="content">
        <ul>
            <% messages.forEach(message => { %>
                <li><%= message.title %>,<%= message.text %>, <%= isLoggedIn ? message.username : '' %></li>
            <% }) %>
        </ul>
    </div>
</body>
</html>